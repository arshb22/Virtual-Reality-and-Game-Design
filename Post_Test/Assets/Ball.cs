using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball : MonoBehaviour
{
    //Move ball
    //Make it so that camera follows behind ball; make sure it still rotates and is in center of frame

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Add force to rigidBody; space.self
    }
}
